import React from 'react'
// import CustomLink from "../CustomLink"

const Settings = ({ data }) => {
  console.log(data)
  return (
    <footer id="footer">
      <h3>
        The Settings page will display your current settings? Is that a
        question?
      </h3>
    </footer>
  )
}

export default Settings
